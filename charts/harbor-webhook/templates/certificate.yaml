{{- if .Values.certManager.enabled }}
apiVersion: {{ .Values.certManager.apiVersion }}
kind: Certificate
metadata:
  name: {{ include "harbor-webhook.fullname" . }}
  labels:
    {{- include "harbor-webhook.labels" . | nindent 4 }}
  namespace: {{ .Release.Namespace }}
spec:
  secretName: {{ include "harbor-webhook.fullname" . }}-certs
  duration: {{ .Values.certManager.duration }}
  renewBefore: {{ .Values.certManager.renewBefore }}
  commonName: {{ include "harbor-webhook.fullname" . }}.{{ .Release.Namespace }}.svc
  dnsNames:
    - {{ include "harbor-webhook.fullname" . }}
    - {{ include "harbor-webhook.fullname" . }}.{{ .Release.Namespace }}
    - {{ include "harbor-webhook.fullname" . }}.{{ .Release.Namespace }}.svc
  issuerRef:
    kind: Issuer
    name: {{ include "harbor-webhook.fullname" . }}-self-signed-issuer
---
apiVersion: {{ .Values.certManager.apiVersion }}
kind: Issuer
metadata:
  name: {{ include "harbor-webhook.fullname" . }}-self-signed-issuer
  labels:
    {{- include "harbor-webhook.labels" . | nindent 4 }}
spec:
  selfSigned: {}
{{- end }}
