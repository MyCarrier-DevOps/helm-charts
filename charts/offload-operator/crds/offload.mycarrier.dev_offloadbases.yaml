---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: offloadbases.offload.mycarrier.dev
spec:
  group: offload.mycarrier.dev
  names:
    kind: OffloadBase
    listKind: OffloadBaseList
    plural: offloadbases
    singular: offloadbase
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.serviceName
      name: Service
      type: string
    - jsonPath: .status.activeOffloads
      name: Offloads
      type: integer
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          OffloadBase defines the base service identity for offload routing.
          The operator reconciles OffloadBase + OffloadRoutes into a single consolidated VirtualService.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OffloadBaseSpec defines the desired state of OffloadBase.
            properties:
              creationPolicy:
                default: CreateOrAdopt
                description: CreationPolicy controls how the operator handles the
                  target VirtualService.
                enum:
                - CreateOrAdopt
                - Adopt
                - Create
                - Orphan
                type: string
              gateways:
                description: Gateways is the list of gateways for the generated VirtualService.
                items:
                  type: string
                minItems: 1
                type: array
              hosts:
                description: Hosts is the list of hosts for the generated VirtualService.
                items:
                  type: string
                minItems: 1
                type: array
              port:
                description: Port is the service port.
                format: int32
                maximum: 65535
                minimum: 1
                type: integer
              responseHeaders:
                additionalProperties:
                  type: string
                description: ResponseHeaders are security headers added to all routes.
                type: object
              retries:
                description: Retries defines the retry policy for routes.
                properties:
                  attempts:
                    format: int32
                    minimum: 1
                    type: integer
                  perTryTimeout:
                    minLength: 1
                    type: string
                  retryOn:
                    minLength: 1
                    type: string
                required:
                - attempts
                - perTryTimeout
                - retryOn
                type: object
              serviceName:
                description: ServiceName is the base service name used for route generation
                  and VS naming.
                minLength: 1
                type: string
              timeout:
                description: Timeout is the route timeout (e.g. "151s").
                type: string
              virtualServiceAnnotations:
                additionalProperties:
                  type: string
                description: VirtualServiceAnnotations are annotations applied to
                  the generated VirtualService.
                type: object
              virtualServiceLabels:
                additionalProperties:
                  type: string
                description: VirtualServiceLabels are labels applied to the generated
                  VirtualService.
                type: object
            required:
            - gateways
            - hosts
            - port
            - serviceName
            type: object
          status:
            description: OffloadBaseStatus defines the observed state of OffloadBase.
            properties:
              activeOffloads:
                description: ActiveOffloads is the number of active OffloadRoutes
                  referencing this base.
                format: int32
                type: integer
              conditions:
                description: Conditions represent the latest available observations
                  of the OffloadBase's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              managedVirtualService:
                description: ManagedVirtualService is the name of the VirtualService
                  managed by this OffloadBase.
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
