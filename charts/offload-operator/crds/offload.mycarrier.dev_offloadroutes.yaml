---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: offloadroutes.offload.mycarrier.dev
spec:
  group: offload.mycarrier.dev
  names:
    kind: OffloadRoute
    listKind: OffloadRouteList
    plural: offloadroutes
    singular: offloadroute
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.environment
      name: Environment
      type: string
    - jsonPath: .spec.offloadBaseRef.name
      name: Base
      type: string
    - jsonPath: .status.active
      name: Active
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          OffloadRoute represents a feature environment's claim on a service for offload routing.
          Lives in the feature namespace alongside the feature's pods and services.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OffloadRouteSpec defines the desired state of OffloadRoute.
            properties:
              destination:
                description: Destination defines where offloaded traffic should be
                  sent.
                properties:
                  host:
                    description: Host is the FQDN of the destination service.
                    minLength: 1
                    type: string
                  port:
                    description: Port is the destination service port.
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                required:
                - host
                - port
                type: object
              environment:
                description: Environment is the feature environment name (e.g. "feature28").
                minLength: 1
                pattern: ^[a-z0-9][a-z0-9-]*$
                type: string
              offloadBaseRef:
                description: OffloadBaseRef references the OffloadBase this route
                  belongs to.
                properties:
                  name:
                    description: Name of the OffloadBase.
                    minLength: 1
                    type: string
                  namespace:
                    description: Namespace where the OffloadBase lives.
                    minLength: 1
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - destination
            - environment
            - offloadBaseRef
            type: object
          status:
            description: OffloadRouteStatus defines the observed state of OffloadRoute.
            properties:
              active:
                description: Active indicates whether this route is currently included
                  in the VirtualService.
                type: boolean
              conditions:
                description: Conditions represent the latest available observations
                  of the OffloadRoute's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
